<!--
LogOS: a host-minimal Agda library for modular dynamic logic systems synthesized by AI
Copyright (C) 2026 AI.IMPACT GmbH
SPDX-License-Identifier: GPL-3.0-only
-->

# Policy Index (Generated)

Generated by `scripts/gen/write_policy_index.sh` from `scripts/check/*_check.sh`.

This is the human-readable index of the `make ci-policy` checks.

## `abstract-deutsch-doc-contract-check`

- Script: `scripts/check/abstract_deutsch_doc_contract_check.sh`
- Run: `make abstract-deutsch-doc-contract-check`
- Policy:

  - The canonical abstract Deutsch-category construction must be documented at a stable anchor.
  - v1.1 defines abstract Deutsch via locality + causality + local reversibility (weaker than unitarity).

## `agda-hygiene-check`

- Script: `scripts/check/agda_hygiene_check.sh`
- Run: `make agda-hygiene-check`
- Policy:

  - Enforce import/open hygiene across LogOS/*.agda.
  - `open import` must use `using`/`hiding`/`renaming`, except for curated public re-exports.
  - `open import ... public` is allowed in `LogOS/API/**`, `LogOS/Prelude.agda`,
  - and thin façade modules that publicly re-export their sibling `Core` module.
  - `open` (non-import) is allowed without `using`/`hiding`/`renaming` (record/module opens are routinely total);
  - prefer explicit lists when opening large namespaces.
  - Forbid anonymous `module _` at the top level.
  - Forbid Agda files over AGDA_HYGIENE_LINE_LIMIT lines (default 700).
  - Forbid duplicate basenames within a namespace branch (top-level + optional
  - two-level prefix + basename), e.g. `Apps/ZFC/Core`. This catches real
  - accidental collisions while allowing intentional domain-specific naming.
  - This is a hard policy check in `ci-policy`; there are no opt-out skip flags.

## `agda-lib-policy-check`

- Script: `scripts/check/agda_lib_policy_check.sh`
- Run: `make agda-lib-policy-check`
- Policy:

  - The library file LogOS.agda-lib is part of the audited build surface.
  - Keep it minimal and pinned: `name: LogOS-LT`, `include: .`, and `flags: --safe` (exactly once each).

## `agda-policy-bundle-check`

- Script: `scripts/check/agda_policy_bundle_check.sh`
- Run: `make agda-policy-bundle-check`
- Policy:

  - bundle the overlapping Agda surface policy checks into one fast pass.
  - Subchecks (use `bash scripts/check/agda_policy_bundle_check.sh --only <name>`):
  - `bridge-contract-check`: Every bridge module must declare its meaning-change contract explicitly (required BRIDGE-CONTRACT header markers + typed `bridgeContract`).
  - `bridges-no-public-reexport-check`: Bridge modules must not re-export imports using `open import ... public` (keep meaning-change surfaces explicit; re-export belongs in API modules).
  - `no-proj12-displayed-check`: In displayed/Σ-totalised code, avoid `proj₁`/`proj₂`; use named projections `base`/`disp`/`baseHom`/`dispHom`.
  - `ports-bridges-import-check`: Bridge modules (`LogOS.Ports.Bridges.*`) must not be imported from arbitrary `LogOS/Ports/**` modules; restrict to bridge entrypoints/wrappers.
  - `ports-funpreorder-usage-check`: `FunPreorder`/`DFunPreorder` are meaning-injection primitives; inside Ports, use them only at the canonical injection points `Locality`.
  - `globalise-import-check`: `LogOS.Ports.Globalise` packages optional global coherence (“classical limit”); keep it quarantined to the canonical modules.
  - `refinement-reasoning-usage-check`: Prefer refinement-chain reasoning combinators; forbid direct `trans⊑`/`≈-trans` and direct `ConPreorder.trans` outside the atomic spine.
  - `theorems-no-eq-check`: Theorem modules are refinement-first and should not mention propositional equality (`≡`).
  - `conpreorder-open-check`: Do not `open ConPreorder` downstream; use projections and reasoning combinators.

## `api-purity-check`

- Script: `scripts/check/api_purity_check.sh`
- Run: `make api-purity-check`
- Policy:

  - `LogOS/API/**` is a curated surface.
  - It may only import from `LogOS.Prelude`, `LogOS.Syntax`, `LogOS.LT`,
  - `LogOS.Ports`, and `LogOS.API`.
  - Default API files must not import explicit strictification lanes.

## `architecture-api-surface-check`

- Script: `scripts/check/architecture_api_surface_check.sh`
- Run: `make architecture-api-surface-check`
- Policy:

  - `LogOS.API.Kernel` must expose the architecture / implementation / façade
  - split explicitly on the curated API surface.

## `architecture-shim-check`

- Script: `scripts/check/architecture_shim_check.sh`
- Run: `make architecture-shim-check`
- Policy:

  - Legacy architecture/implementation/law shim module paths have been retired.
  - Reintroducing any old-name shim requires an explicit policy change first.

## `archive-boundary-check`

- Script: `scripts/check/archive_boundary_check.sh`
- Run: `make archive-boundary-check`
- Policy:

  - Historical material must not be treated as part of the live repository root.
  - The live repo root must not carry stale archive entrypoints such as `TEMP`
  - or legacy zip bundles.
  - If an in-repo archive subtree is present, its entrypoints must say that they
  - are historical and out of contract.

## `atomic-spine-import-check`

- Script: `scripts/check/atomic_spine_import_check.sh`
- Run: `make atomic-spine-import-check`
- Policy:

  - The atomic LT spine modules are intentionally small and strictly layered.
  - Their allowed imports are fixed and minimal to prevent accidental meaning leaks.

## `canonical-architecture-import-check`

- Script: `scripts/check/canonical_architecture_import_check.sh`
- Run: `make canonical-architecture-import-check`
- Policy:

  - Canonical architecture/implementation modules may not depend on compatibility
  - shims.
  - Canonical LT/Ports internals should import the pristine `Core` lane directly
  - when a façade wrapper exists, except at explicitly curated façade surfaces.

## `canonical-legacy-free-check`

- Script: `scripts/check/canonical_legacy_free_check.sh`
- Run: `make canonical-legacy-free-check`
- Policy:

  - Canonical architecture/implementation/law surfaces must be free of
  - retired legacy names.

## `centering-quote-vocabulary-check`

- Script: `scripts/check/centering_quote_vocabulary_check.sh`
- Run: `make centering-quote-vocabulary-check`
- Policy:

  - `QuoteConeMMP` is an internal implementation-lane name.
  - Public docs, checks, and curated API surfaces must use the centering-facing
  - vocabulary instead of surfacing the old packaging term.

## `check-scripts-meta-policy-check`

- Script: `scripts/check/check_scripts_meta_policy_check.sh`
- Run: `make check-scripts-meta-policy-check`
- Policy:

  - Every `scripts/check/*_check.sh` must follow the standard check-script shape:
  - SPDX header, `set -euo pipefail`, `CHECK_NAME="..."`, and `scripts/lib/check_common.sh`.

## `ci-workflow-policy-check`

- Script: `scripts/check/ci_workflow_policy_check.sh`
- Run: `make ci-workflow-policy-check`
- Policy:

  - CI must be reproducible: pin third-party actions by full commit SHA and avoid `*-latest` runners.
  - CI must be strict: the default CI lane must enforce the core gate, and at least one
  - workflow must enforce the cold full gate (`make check-all`).
  - CI must be hardened: minimal permissions, no credential persistence, strict bash defaults, and
  - cancel in-progress runs on the same ref.

## `claim-stamp-check`

- Script: `scripts/check/claim_stamp_check.sh`
- Run: `make claim-stamp-check`
- Policy:

  - High-risk docs must carry explicit claim stamps (`<!-- CLAIM-STAMP: ... -->`).
  - Every claim stamp must have a valid anchor into an existing file+symbol/label/heading.

## `coherence-law-check`

- Script: `scripts/check/coherence_law_check.sh`
- Run: `make coherence-law-check`
- Policy:

  - The main coherence risk in the law layer is relation-split duplication:
  - restating the same law separately for incompatible notions such as `≈`,
  - `⊑`, or strict equality.
  - The preferred architecture is to state a law once when possible and index it
  - by `LogOS.LT.Coherence.CohMode` / `CohRel`, exposing refinement-facing
  - modes publicly and leaving strict witnesses quarantined.
  - This check is heuristic. It rewards visible mode-indexed law declarations
  - and tracks unindexed relation-split law families in default lanes.

## `core-role-check`

- Script: `scripts/check/core_role_check.sh`
- Run: `make core-role-check`
- Policy:

  - Core modules should not import `Definitional` or `Strictification` lanes
  - unless explicitly allowlisted.

## `curated-docs-index-coverage-check`

- Script: `scripts/check/curated_docs_index_coverage_check.sh`
- Run: `make curated-docs-index-coverage-check`
- Policy:

  - Curated literate docs must be discoverable from the generated docs inventory.
  - Every `docs/{Core,Results,Patterns,Interpretations}/**/*.lagda.md` file must be referenced from `docs/Generated/Docs_Index.md`.

## `dangerous-pragmas-check`

- Script: `scripts/check/dangerous_pragmas_check.sh`
- Run: `make dangerous-pragmas-check`
- Policy:

  - Keep the soundness story simple: forbid pragma patterns that bypass termination/positivity or enable rewriting/FFI.
  - Enforce the same rule in docs by scanning fenced `agda` blocks.

## `design-index-coverage-check`

- Script: `scripts/check/design_index_coverage_check.sh`
- Run: `make design-index-coverage-check`
- Policy:

  - `docs/Patterns/All.lagda.md` is the curated entrypoint for design notes.
  - Every `docs/Patterns/*.lagda.md` note (except the index itself) must be referenced from the index.

## `doc-import-discipline-check`

- Script: `scripts/check/doc_import_discipline_check.sh`
- Run: `make doc-import-discipline-check`
- Policy:

  - public-facing docs (docs/*.md outside docs/Core/Spec/**) should
  - demonstrate the curated API surface rather than deep internal imports.

## `doc-reference-check`

- Script: `scripts/check/doc_reference_check.sh`
- Run: `make doc-reference-check`
- Policy:

  - Repo-facing docs must stay link-honest: any repo-path-like reference in prose must point to an existing file.
  - Markdown links should stay portable across local previewers: use relative paths, not repo-root `/...` links.
  - This prevents “pure legacy” pointers after refactors/moves.

## `docs-taxonomy-check`

- Script: `scripts/check/docs_taxonomy_check.sh`
- Run: `make docs-taxonomy-check`
- Policy:

  - The live repository uses the current semantic docs taxonomy.
  - Retired audience-split or pre-migration doc path families may remain only
  - in historical material kept out of the live repository.

## `equality-quarantine-check`

- Script: `scripts/check/equality_quarantine_check.sh`
- Run: `make equality-quarantine-check`
- Policy:

  - Curated default LT/API surfaces must not export `≡`-valued declarations.
  - Equality exports belong only in `Strictification`, `Definitional`, `Prelude`,
  - or executable `Checks` lanes.

## `generated-docs-fresh-check`

- Script: `scripts/check/generated_docs_fresh_check.sh`
- Run: `make generated-docs-fresh-check`
- Policy:

  - Generated docs committed in-tree must match their generators.
  - After changes, run `make views-all`, `make design-all`, `make layer-order-legend`,
  - `make module-index`, `make policy-index`, `make docs-index`,
  - `make claim-stamp-index`, `make published-surface-index`,
  - `make zfc-upgrade-index`, `make equality-surface-map`,
  - `make strictification-inventory`, `make architecture-clarity-report`,
  - and `make lt-ports-import-graph`.

## `host-import-check`

- Script: `scripts/check/host_import_check.sh`
- Run: `make host-import-check`
- Policy:

  - `LogOS/Host/**` is the host-wrapper layer.
  - Everywhere else should access host shims via `LogOS.Prelude(.agda)` and any
  - explicitly approved Prelude support modules.
  - This makes host contact points structurally trivial to audit.

## `host-surface-check`

- Script: `scripts/check/host_surface_check.sh`
- Run: `make host-surface-check`
- Policy:

  - Builtins and primitive universe machinery are confined to `LogOS/Host/**`.
  - Everything else must depend on the curated host wrappers (also enforced for docs code blocks).

## `law-port-forgetfuls-doc-check`

- Script: `scripts/check/law_port_forgetfuls_doc_check.sh`
- Run: `make law-port-forgetfuls-doc-check`
- Policy:

  - Any `*2Cat.agda` module that defines a law-port via `LawDisplayed`/`LawDisplayedOn` must provide
  - an explicit `forget…` projection (unless it carries an explicit justification marker).
  - This keeps “data + law” ports usable and prevents unprojectable “sealed” constructions.

## `law-surface-check`

- Script: `scripts/check/law_surface_check.sh`
- Run: `make law-surface-check`
- Policy:

  - The repository tracks an explicit law surface as a CI trend metric.
  - This check is heuristic: it scans top-level Agda declarations for
  - law-shaped names (`Law`, `Laws`, `Coherence`, `-law`, `-mono`, ...).
  - It guards against accidental regressions in bundled/proved structure, but it
  - is not a semantic adequacy theorem.

## `layer-order-check`

- Script: `scripts/check/layer_order_check.sh`
- Run: `make layer-order-check`
- Policy:

  - import layering follows docs/Core/Architecture/Diagram.lagda.md and the shared
  - layer order in scripts/lib/layers.sh.

## `legacy-architecture-name-check`

- Script: `scripts/check/legacy_architecture_name_check.sh`
- Run: `make legacy-architecture-name-check`
- Policy:

  - Retired architecture names are tracked after shim removal.
  - This check is inventory-only: it reports residual occurrences in canonical
  - and integration lanes, but does not fail CI.

## `license-headers-check`

- Script: `scripts/check/license_headers_check.sh`
- Run: `make license-headers-check`
- Policy:

  - Every allowlisted text file must contain the standard per-file license header (title + copyright + SPDX).
  - Tracked binary assets must be handled explicitly (they cannot carry per-file text headers).

## `local-boundary-map-check`

- Script: `scripts/check/local_boundary_map_check.sh`
- Run: `make local-boundary-map-check`
- Policy:

  - Locality boundary maps (`map∂`) must be pointwise/canonical (identity or pointwise map combinators).
  - Non-pointwise meaning changes must be quarantined (`LogOS/Ports/Quarantine/**`) or explicitly bridged.

## `local-boundary-usage-check`

- Script: `scripts/check/local_boundary_usage_check.sh`
- Run: `make local-boundary-usage-check`
- Policy:

  - `LocalBoundary` is the canonical (dependent-first) locality boundary.
  - `DependentLocalBoundary` is the retired legacy spelling and must not appear.

## `log-basis-usage-check`

- Script: `scripts/check/log_basis_usage_check.sh`
- Run: `make log-basis-usage-check`
- Policy:

  - LOG-basis port categories may not be imported from internal LT/Ports code by default.
  - Use LOGᴳ-basis categories internally, and allow LOG imports only for explicit physics/Deutsch
  - allowlisted modules.

## `lt-theorem-boundary-check`

- Script: `scripts/check/lt_theorem_boundary_check.sh`
- Run: `make lt-theorem-boundary-check`
- Policy:

  - Non-theorem LT modules may only import theorem modules when the dependency is
  - explicitly recorded as load-bearing architecture.

## `makefile-guardrails-check`

- Script: `scripts/check/makefile_guardrails_check.sh`
- Run: `make makefile-guardrails-check`
- Policy:

  - The repository build must stay host-minimal and strict: `--no-libraries --safe` and warnings-as-errors.
  - CI gate shape is stable: `check-all` cleans then runs the telemetry-backed full warm gate; `ci` runs the core warm gate.

## `no-cloning-doc-mention-check`

- Script: `scripts/check/no_cloning_doc_mention_check.sh`
- Run: `make no-cloning-doc-mention-check`
- Policy:

  - The Deutsch no-cloning sidecar must be discoverable from documentation (not only from code).

## `no-hidden-decoratedthin2cat-check`

- Script: `scripts/check/no_hidden_decoratedthin2cat_check.sh`
- Run: `make no-hidden-decoratedthin2cat-check`
- Policy:

  - Port totalisations must be explicit and discoverable: do not define new `= DecoratedThin2Cat ...`
  - outside `*2Cat.agda` modules (except discipline gates).
  - This prevents bypassing coverage/enforcement by hiding port categories in other files.

## `no-manual-displayed-product-check`

- Script: `scripts/check/no_manual_displayed_product_check.sh`
- Run: `make no-manual-displayed-product-check`
- Policy:

  - Independent port composition must use `ProductDisplayed`.
  - Avoid hand-rolled displayed products (a common drift anti-pattern). If a manual product is truly required,
  - add a local justification marker `MANUAL-PRODUCT-JUSTIFICATION:` in the file.

## `no-redundant-hiding-check`

- Script: `scripts/check/no_redundant_hiding_check.sh`
- Run: `make no-redundant-hiding-check`
- Policy:

  - Forbid empty `hiding ()` clauses in `open` / `open import` statements.
  - Forbid empty `using ()` clauses in `open` / `open import` statements.
  - Rationale: both are no-ops; keeping them around is pure noise and tends to accumulate as a “satisfy CI”
  - workaround rather than an intentional dependency boundary.

## `no-tabs-check`

- Script: `scripts/check/no_tabs_check.sh`
- Run: `make no-tabs-check`
- Policy:

  - Forbid tab characters in repository text sources to avoid invisible formatting drift in code and docs.

## `no-with-k-check`

- Script: `scripts/check/no_with_k_check.sh`
- Run: `make no-with-k-check`
- Policy:

  - Forbid the `with-K` option in repository-level checked-in build/test configuration.

## `pack-story-contract-check`

- Script: `scripts/check/pack_story_contract_check.sh`
- Run: `make pack-story-contract-check`
- Policy:

  - Each application pack (`LogOS/Apps/*`) must declare its public story in `LogOS/Apps/*/All.agda`:
  - `Entrypoints:`, `Implemented now:`, and `Planned:`.

## `packaging-equality-check`

- Script: `scripts/check/packaging_equality_check.sh`
- Run: `make packaging-equality-check`
- Policy:

  - Template packaging equalities like `displayed≡`, `withPort≡`, and `forget≡`
  - belong only in explicit `Definitional`, `Strictification`, or `Checks`
  - lanes.
  - The default surface may use the underlying template constructors, but must
  - not expose or depend on these packaging-equality names directly.

## `polarity-reminder-check`

- Script: `scripts/check/polarity_reminder_check.sh`
- Run: `make polarity-reminder-check`
- Policy:

  - High-risk polarity-sensitive modules must carry a `Direction note:` or
  - `Polarity note:` reminder near the module header.

## `policy-coverage-check`

- Script: `scripts/check/policy_coverage_check.sh`
- Run: `make policy-coverage-check`
- Policy:

  - Every `scripts/check/*_check.sh` must be runnable as a `make <check-name>` target and be wired into `make ci-policy`,
  - unless explicitly allowlisted with a justification.

## `ports-as-displayed-coverage-check`

- Script: `scripts/check/ports_as_displayed_coverage_check.sh`
- Run: `make ports-as-displayed-coverage-check`
- Policy:

  - Every canonical `*2Cat.agda` port totalisation must be typechecked through the
  - corresponding canonical discipline gate.
  - Explicit strictification ports are covered by a separate strictification gate.

## `ports-as-displayed-design-contract-check`

- Script: `scripts/check/ports_as_displayed_design_contract_check.sh`
- Run: `make ports-as-displayed-design-contract-check`
- Policy:

  - The ports-as-displayed design note must document the canonical constructors, so contributors
  - copy the normal form rather than inventing bespoke encodings.

## `ports-as-displayed-doc-contract-check`

- Script: `scripts/check/ports_as_displayed_doc_contract_check.sh`
- Run: `make ports-as-displayed-doc-contract-check`
- Policy:

  - The enforced ports-as-displayed design note must explicitly document the closed loop:
  - canonical discipline gates + coverage check + API witnesses.

## `ports-uniform-import-check`

- Script: `scripts/check/ports_uniform_import_check.sh`
- Run: `make ports-uniform-import-check`
- Policy:

  - (Policy not annotated in script comments or error messages.)

## `postulate-policy-check`

- Script: `scripts/check/postulate_policy_check.sh`
- Run: `make postulate-policy-check`
- Policy:

  - `postulate` is forbidden by default.
  - Allowed exceptions must be whitelisted and carry a structured justification block (also enforced in docs code blocks).

## `proof-coverage-check`

- Script: `scripts/check/proof_coverage_check.sh`
- Run: `make proof-coverage-check`
- Policy:

  - The cold CI umbrella should cover the full host-mechanized Agda surface
  - (`LogOS/**/*.agda` plus `docs/**/*.lagda.md`).
  - The repository tracks an "assumption-light" Agda sub-surface: modules whose
  - import closure stays outside ledger-declared assumption usage.
  - This second metric is a syntactic CI trend metric, not a semantic theorem:
  - assumption providers come from the canonical assumptions ledger and direct
  - usage is detected symbol-wise after stripping Agda comments.

## `public-portstack-uniqueness-check`

- Script: `scripts/check/public_portstack_uniqueness_check.sh`
- Run: `make public-portstack-uniqueness-check`
- Policy:

  - Default API stack exports must be uniqueness-first.
  - Raw `PortStack` / `HasPort` / duplicate-tag shadowing surfaces must not be
  - re-exported from `LogOS/API/**`.
  - Explicit raw/shadowing access belongs in non-default LT lanes.

## `publication-default-lane-check`

- Script: `scripts/check/publication_default_lane_check.sh`
- Run: `make publication-default-lane-check`
- Policy:

  - Published/default LT, Ports, and API surfaces should not import
  - `Definitional` or `Strictification` lanes directly unless they sit on an
  - explicit shrinking allowlist with a one-line justification.

## `publication-generated-index-check`

- Script: `scripts/check/publication_generated_index_check.sh`
- Run: `make publication-generated-index-check`
- Policy:

  - The generated publication index must stay in sync with the explicit
  - publication manifest and allowlists.

## `publication-surface-check`

- Script: `scripts/check/publication_surface_check.sh`
- Run: `make publication-surface-check`
- Policy:

  - Publication tiers are explicit, not inferred from directory names.
  - Published entries must be curated surfaces with a navigator (`README.md` or a local `All` entrypoint module for directories).
  - Historical and optional tooling subtrees must never be classified as published.

## `publication-wording-check`

- Script: `scripts/check/publication_wording_check.sh`
- Run: `make publication-wording-check`
- Policy:

  - Published surfaces must not advertise provisional or throwaway status with
  - words like `TEMP`, `WIP`, `placeholder`, or unqualified `experimental`.
  - Narrow exceptions are tracked in an explicit wording allowlist.

## `quarantine-import-check`

- Script: `scripts/check/quarantine_import_check.sh`
- Run: `make quarantine-import-check`
- Policy:

  - Meaning-changing constructions under `LogOS.Ports.Quarantine/**` may only be imported by Quarantine itself
  - and curated bridge modules.

## `quarantine-story-doc-check`

- Script: `scripts/check/quarantine_story_doc_check.sh`
- Run: `make quarantine-story-doc-check`
- Policy:

  - Documentation must name the meaning-injection / meaning-change compartments:
  - `LogOS/Ports/Quarantine/**` and `LogOS/Ports/Bridges/**`.
  - This prevents contributors from smuggling meaning changes into ordinary ports/adapters without noticing.

## `reachability-check`

- Script: `scripts/check/reachability_check.sh`
- Run: `make reachability-check`
- Policy:

  - No orphaned core modules: every non-Host core Agda module must be reachable
  - from explicit policy/check roots or from the canonical spec roots.

## `refinement-first-surface-check`

- Script: `scripts/check/refinement_first_surface_check.sh`
- Run: `make refinement-first-surface-check`
- Policy:

  - The curated LT/API surfaces are refinement-first.
  - Strict/equality surfaces must stay behind explicit `Strictification`,
  - `Definitional`, or `Shadowing` lanes.

## `relation-status-naming-check`

- Script: `scripts/check/relation_status_naming_check.sh`
- Run: `make relation-status-naming-check`
- Policy:

  - High-risk public theorem surfaces must declare relation status explicitly.
  - New `_≈_`, `_⊑_`, `_≼_`, `_≤s_`, and `≡` theorem results in covered modules must be
  - manifest-listed before they land.
  - Canonical theorem names must end with a suffix matching their relation mode.

## `required-docs-check`

- Script: `scripts/check/required_docs_check.sh`
- Run: `make required-docs-check`
- Policy:

  - A small set of documentation capstones are part of the audited surface and must exist.

## `rewrite-hotspot-check`

- Script: `scripts/check/rewrite_hotspot_check.sh`
- Run: `make rewrite-hotspot-check`
- Policy:

  - `rewrite` is banned in the main semantic hotspots where relation-valued helper
  - lemmas should carry the proof burden.
  - In the ZFC semantics core, any remaining `rewrite` must be explicitly marked
  - with `-- rewrite-justified:<lemma-name>`.

## `root-hygiene-check`

- Script: `scripts/check/root_hygiene_check.sh`
- Run: `make root-hygiene-check`
- Policy:

  - All root-level Agda modules must be explicitly allowlisted.
  - Repo-root entries must stay within the maintained boundary contract.
  - Junk artifacts such as `__MACOSX`, `._*`, `.DS_Store`, `*.pyc`, and
  - `__pycache__` must not ship outside ignored build directories.

## `safe-options-check`

- Script: `scripts/check/safe_options_check.sh`
- Run: `make safe-options-check`
- Policy:

  - Every Agda module and literate Agda doc must opt into `{-# OPTIONS --safe #-}`.
  - No other `{-# OPTIONS ... #-}` pragmas are allowed in the repository-safe surface.

## `shadowing-lane-check`

- Script: `scripts/check/shadowing_lane_check.sh`
- Run: `make shadowing-lane-check`
- Policy:

  - `LogOS.LT.Ports.PortStack.Raw` is an explicit raw/internal lane.
  - Docs, curated API modules, and app surfaces must not import it directly.

## `spec-ref-check`

- Script: `scripts/check/spec_ref_check.sh`
- Run: `make spec-ref-check`
- Policy:

  - Every `LogOS/LT/**` module must declare exactly one `-- SpecRef:` line.
  - `docs/Core/Spec/LogicalTransformers.lagda.md` must import every `LogOS.LT.*` module (sync guard).

## `spine-doc-contract-check`

- Script: `scripts/check/spine_doc_contract_check.sh`
- Run: `make spine-doc-contract-check`
- Policy:

  - The documentation must present the canonical conceptual spine in order
  - (ConPreorder → View → Kernel → KernelHom → LOG → DisplayedThin2Cat → DecoratedThin2Cat),
  - so the library stays self-similar and discoverable.

## `stale-allowlists-check`

- Script: `scripts/check/stale_allowlists_check.sh`
- Run: `make stale-allowlists-check`
- Policy:

  - Allowlists are quarantines: every entry must be justified, unique, and non-stale.
  - Do not hide checks from the CI baseline via policy_coverage_allowlist (must stay empty).
  - Do not quarantine layer-order violations (layer_order_allowlist must stay empty).
  - Postulate allowlist is permitted but must be minimal and justified.

## `strictification-boundary-check`

- Script: `scripts/check/strictification_boundary_check.sh`
- Run: `make strictification-boundary-check`
- Policy:

  - Strictification/definitional/raw-shadowing imports must not silently drift
  - into default LT/Ports/Apps lanes.
  - Any such dependency outside explicit quarantine lanes must be allowlisted
  - with a one-line architectural justification.

## `strictification-contract-check`

- Script: `scripts/check/strictification_contract_check.sh`
- Run: `make strictification-contract-check`
- Policy:

  - Curated strictification modules should advertise one explicit strict object,
  - the downgrade path back to refinement, and a named law/anchor.

## `strictification-doc-import-check`

- Script: `scripts/check/strictification_doc_import_check.sh`
- Run: `make strictification-doc-import-check`
- Policy:

  - Literate docs are refinement-first by default.
  - Docs may import `*Strictification*` modules only when the doc file name
  - itself advertises that mode (`Strict`, `Extensional`, or `ClassicalLimit`),
  - except for the canonical LT spec import inventory.

## `subtree-navigator-check`

- Script: `scripts/check/subtree_navigator_check.sh`
- Run: `make subtree-navigator-check`
- Policy:

  - Large maintained subtrees must expose a local navigator.
  - For directories under `LogOS/API`, `LogOS/Checks`, `LogOS/LT`,
  - `LogOS/Ports`, and `LogOS/Apps`, a directory with at least 8 direct Agda
  - files must contain either a sibling README or a local `All` navigator module.

## `theorems-catalog-check`

- Script: `scripts/check/theorems_catalog_check.sh`
- Run: `make theorems-catalog-check`
- Policy:

  - Every theorem module under `LogOS/LT/Theorems/**` must be explicitly catalogued by a public re-export in:
  - `LogOS/API/Theorems/Core.agda`
  - `LogOS/API/Theorems/Strictification.agda`
  - Exception: files ending in `*Impl.agda` are treated as internal packaging modules.
  - `Cone` and `MinimalModelProgram` are no longer valid theorem catalogs.

## `tooling-contract-check`

- Script: `scripts/check/tooling_contract_check.sh`
- Run: `make tooling-contract-check`
- Policy:

  - Each current tool subproject under `tools/metamath/**` must declare purpose,
  - dependencies, a smoke command, and outputs.
  - The canonical smoke entrypoints live under `scripts/metamath/**`.

## `trivial-boundary-smell-check`

- Script: `scripts/check/trivial_boundary_smell_check.sh`
- Run: `make trivial-boundary-smell-check`
- Policy:

  - Hard gate: forbid *local* `ConPreorder` constructions that collapse
  - refinement to equality (`_⊑_ = _≡_`) or that use a unit carrier as a boundary.
  - Canonical terminal/unit boundary is `LogOS.LT.ConPreorder.Unit`; canonical
  - equality-refinement boundary is `LogOS.LT.ConPreorder.Discrete`. This check
  - only applies to redefinitions outside those modules.
  - These can be useful for tiny scaffolds, but they are usually a smell in a
  - refinement-first codebase: they erase information and often hide where the
  - real observation/refinement structure should live.

## `type-theory-shell-check`

- Script: `scripts/check/type_theory_shell_check.sh`
- Run: `make type-theory-shell-check`
- Policy:

  - `LogOS.LT.TypeTheory` is a shell view over deeper LT implementations.
  - Non-quarantine shell modules should stay re-export only; substantial logic
  - belongs in the underlying LT layers.

## `unicode-invisibles-check`

- Script: `scripts/check/unicode_invisibles_check.sh`
- Run: `make unicode-invisibles-check`
- Policy:

  - forbid invisible/bidi control characters in source and docs.

## `unsafe-options-check`

- Script: `scripts/check/unsafe_options_check.sh`
- Run: `make unsafe-options-check`
- Policy:

  - Forbid `--unsafe` and `--allow-unsolved-metas` in OPTIONS pragmas (also enforced in docs code blocks).

## `zfc-canonical-bridge-purity-check`

- Script: `scripts/check/zfc_canonical_bridge_purity_check.sh`
- Run: `make zfc-canonical-bridge-purity-check`
- Policy:

  - The theorem-facing iterative-tree canonical bridge must stay free of
  - same-stage completion machinery.
  - In particular, `LogOS/Apps/ZFC/Models/IterativeSetTree/CanonicalBridge.agda`
  - must not import completion modules or re-expose completion-layer names.

## `zfc-canonical-slice-purity-check`

- Script: `scripts/check/zfc_canonical_slice_purity_check.sh`
- Run: `make zfc-canonical-slice-purity-check`
- Policy:

  - The canonical iterative-tree successor slice must stay free of same-stage
  - completion machinery.
  - In particular, `LogOS/Apps/ZFC/Models/IterativeSetTree/CumulativeHierarchy.agda`
  - must not import the one-stage completion assembler or re-expose
  - completion-layer names.

## `zfc-legacy-quarantine-check`

- Script: `scripts/check/zfc_legacy_quarantine_check.sh`
- Run: `make zfc-legacy-quarantine-check`
- Policy:

  - The iterative-tree legacy pair packaging is quarantined compatibility only.
  - Curated surfaces, docs, and examples must not mention it.

## `zfc-upgrade-discipline-check`

- Script: `scripts/check/zfc_upgrade_discipline_check.sh`
- Run: `make zfc-upgrade-discipline-check`
- Policy:

  - ZFC assumption/upgrade packs must be manifest-listed and called out in the
  - canonical assumptions ledger.
  - Derived-theorem upgrade entries additionally need an explicit claim-stamp
  - anchor in the ledger/docs story.

